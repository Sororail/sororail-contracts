[package]
name = "sororail-integration-tests"
description = "Cross-contract integration tests. Not published."
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
doctest = false

[dependencies]

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
sororail-common = { workspace = true, features = ["testutils"] }
sororail-escrow = { path = "../contracts/escrow" }
sororail-stream = { path = "../contracts/stream" }
sororail-vesting = { path = "../contracts/vesting" }
sororail-recurring = { path = "../contracts/recurring" }
sororail-batch-payout = { path = "../contracts/batch_payout" }

[lints]
workspace = true
