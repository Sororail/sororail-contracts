# Funding Claiming Status

`FUNDING.json` still uses the zero address while the Sororail governance wallet is not finalized. The placeholder is intentional for now and must not be treated as a claimable Drips owner address.

Before enabling funding claims, replace `drips.ethereum.ownedBy` with the finalized owner wallet and remove the deferred claiming note from the funding metadata.